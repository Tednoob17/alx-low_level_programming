#include "main.h"
/**
 * _abs - return abs value
 * @n : number to check
 * Return:0 or 1
 */
int _abs(int n)
{
	if (n < 0)
	{
		n = -n;
		return (n);
	}
	else
	{
		return (n);
	}

	_putchar ('\n');
}

