#include "main.h"
/*
*main - check the code.
*
*
*Return : Always success error otherwise
*/
int _abs(int n)
{
	if ( n < 0 )
	{
		n = -n;
		return (n);
	}
	else
	{
		return (n);
	}

	_putchar ('\n');
}

